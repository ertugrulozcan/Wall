<UserControl
    x:Class="Wall.UserControls.Cell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wall.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="40">
    
    <Grid>
        <Border BorderBrush="White" BorderThickness="0">
            <Grid>
                <Canvas x:Name="ShapeCanvas">
                    <Polygon x:Name="Hexagon"/>
                </Canvas>

                <TextBlock x:Name="CoordinateTB" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
